# myfirstreact

**Lab objective:** Set up a React environment using `create-react-app` and render a
heading.

## What it does
`App.js` renders a single `<h1>` with the text
"welcome to the first session of React".

## Run
```bash
npm install
npm start
```
Open `http://localhost:3000`.
